/**
 * @file app.cpp
 * @brief Application / test code for devkit (C++17).
 * Same app_init() / app_run() as C boards; symbols use C linkage via app.hpp.
 */

#include "app/app.hpp"

/* Add C++ modules under app/devkit/, list .cpp in CMakeLists (see messaging/). */

void app_init(void)
{
}

void app_run(void)
{
	/* Devkit test loop or tasks */
}
